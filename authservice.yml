server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/auth_db
    username: root
    password: ""
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    
    jwt:
  secret: my-super-secret-key-which-is-at-least-32-characters-long
  expiration-ms: 3600000

